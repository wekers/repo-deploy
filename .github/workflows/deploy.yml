name: Deploy Fake
on:
    workflow_call: 
jobs:
    deploy-kubernetes:
        runs-on: ubuntu-latest
        steps:
            - name: Deploy no ambiente
              run: echo "Executando o deploy no ambiente de ${{ secrets.AMBIENTE_ENTREGA }}"
            - name: Curl Deploy no ambiente
              run: "curl -X POST https://webhook.site/8170a92a-2d71-4bbf-8e6e-a8df07bfeb03 -d '{ ambiente: ${{ secrets.AMBIENTE_ENTREGA}}}' "
